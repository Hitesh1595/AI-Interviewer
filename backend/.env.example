# --- LLM provider (OpenAI) ---
# "openai" | "mock" | "auto"
LLM_PROVIDER=openai

# OpenAI — https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-4o-mini          # evaluation / scoring
OPENAI_REALTIME_MODEL=gpt-realtime-2   # live voice interview
OPENAI_VOICE=alloy                      # alloy | echo | shimmer | ...

# Optional: a GitHub token raises the public API rate limit (60 -> 5000 req/hr).
GITHUB_TOKEN=

# SQLite database URL
DATABASE_URL=sqlite:///./ai_interviewer.db

# CORS origins for the frontend dev server (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Admin auth (dashboard) — CHANGE THESE ---
ADMIN_PASSWORD=changeme
ADMIN_SECRET=dev-admin-secret-change-me
